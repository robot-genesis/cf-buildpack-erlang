#!/bin/bash
# bin/release <build-dir>

echo ""

# TODO: Run escript that reads rebar.config -> {relx, [{release, {<name>, <version>}, []}]}

#echo "default_process_types:"
#echo "  web: /home/vcap/app/_build/default/rel/<name>/bin/<name> foreground"
